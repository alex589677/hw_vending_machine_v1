файл snack.h
Обьявляется класс Snack с модификаторами доступа
private и public. Присутствует одно поле класса name 
типа string, в секции private.
В секции public обьявляются 4 свойства.
Конструктор по умолчанию. Параметризованный конструктор.
Конструктор копирования. Геттер для поля класса. 
файл snack.cpp
Первая функция это параметризованный конструктор с одним
аргументом name типа string. Присвоение аргумента полю
класса.
Вторая функция конструктор копирования с ссылочным 
аргументом типа Snack. Присвоение полю класса аттрибуту
передаваемого обьекта.
Третья функция геттер возвращает поле класса.
файл snackslot.h
В файл подключаются заголовочный файл #include "snack.h"
 и библиотека векторов #include <vector>.
Обьявляется класс SnackSlot с модификаторами доступа.
Поля класса целочисленная переменная top и переменная 
snackSlots(вектор) с типом Snack для упаковки снеков.
В секции public обьявлены 4 функции.
Один конструктор по умолчанию. Второй парамертизованный
контсруктор. Функция добавления снека. И функция перечисления
снеков находящихся в векторе.
файл snackslot.cpp
В файл подключается заголовочный файл #incldue "snackslot.h".
Реализация параметризованного конструктора с параметром size
типа int. Присвоение со списком инициализации полю класса
параметра.
Функция добавления снека в обьект slot(созданного в файле
main.cpp). Добавляется разыменованный указатель в конец
вектора snackSlots.
И функция displaySnackSlot() в качестве индикатора что снеки
добавились в контейнер. Использовался оператор for each с 
выводом каждого обьекта находящегося в контейнере через
пробел.
файл vendingmachine.h
Добавил макрос выполнения файла единожды.
Добавил библиотеку вектор #include <vector> и заголовочный
файл snackslot #include "snackslot".
Обьявил класс VendingMachine. С модификаторами доступа.
Два поля класса целочисленная size_ и переменная vendingSlots
(вектор) с типом SnackSlot.
Свойства класса в общедоступной секции.
Конструктор по умолчанию. Параметризованный конструктор.
Функция addSlot(SnackSlot*) с параметром слот.
Функция getEmptySlotsCount().
файл vendingmachine.cpp
Подключается заголовочный файл #include "vendingmachine.h".
Конструктор предваряется именем класса и оператором
разрешения области видимости (::). С помощью списка
инициализации присваивается поле класса передаваемому
 параметру.
Функция addSlot(SnackSlot*). В ней в вектор vendingSlots 
добавляется разыменованный указатель типа SnackSlot.
Функция getEmptySlotsCount() у меня в файле main.cpp не
работает.
файл main.cpp
Устанавливаю поддержку кириллицы с помощью функции setlocale.
Создаю указатель bounty и выделяю память типа Snack в куче,
 с передаваемым параметром "Bounty". В конце удаляю
указатель bounty посредством оператора delete.
Создаю второй указатель snickers и выделяю память типа Snack 
в куче, с передаваемым параметром "Snickers". В конце удаляю
 указатель snickers посредством оператора delete.
Далее создаю указатель slot типа SnackSlot обьемом 10 
экземляров динамически. Потом добавляю в слот два указателя
с помощью функции addSnack().
Далее обьявляю и инициализирую указатель типа VendingMachine,
 на 2 обьекта. Добавляю в указатель machine слот с помощью
функции addSlot(slot). И закомментированная функция 
getEmptySlotsCount().
Также удаляю указатель из кучи с помощью оператора delete.